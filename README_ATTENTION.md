# AGILLM attention branch

This branch contains an AGILLM-specific attention dispatcher, correctness tests,
a single-file runtime candidate builder and a CUDA benchmark harness.

Start with [the design and promotion notes](docs/attention_backend.md).

Nothing in this branch automatically changes a running trainer.
